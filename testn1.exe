#include <stdio.h>
int kt(int n);
int kt (int n)
{
    int i,dem;
    dem=0;
    i=0;
    for (i=1; i<=n/2; i++) {
        if (n%i ==0)
            dem++;
    }
    if (dem==1) {
        return n;
        printf("\n");
    }
    return 0
}
int main ()
{
    int  check;
    scanf("%d",&check);
    kt (check);
    return 0;
    
}