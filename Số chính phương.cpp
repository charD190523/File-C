#include <stdio.h>
int main ()
{
    int i,n;
    scanf("%d",&n);
    scanf("%d",&k);
    while (i++<n)
    {
        
        if (j*j<=k)
        {
            if(j*j==k)
                printf("%s",YES);
            else
                printf("%s",NO);
            
            k++;
            
        }


    }
}